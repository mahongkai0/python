

插入排序

描述：将数据插入到已经有序的数据当中，从而得到新的有序数据
默认首元素自然有序 取下一个元素 对已经有序的数据从后向前扫描对比
如果扫描到的有序数据大于取出数据，则该有序数据后移，
如果扫描到的有序数据小于等于取出数据，则在该有序数据后面插入取出数据
如果所有有序数据均大于取出数据，（即遍历玩所有有序数据），则在有序数据首部插入取出数据
重复上步骤，直到处理完所有数据为止


快速排序

描述：首先 任取一个数据（通常取第一个）作为一个关键数据 然后所有小于关键数据的放在其前面 
所以大于他的数据放在其后面 这样作为一趟快速排序
通过一样快速排序将数据分割为两个独立的部分 然后按照该方法对两部分数据进行快速排序 直到所有数据有序为止
























































































